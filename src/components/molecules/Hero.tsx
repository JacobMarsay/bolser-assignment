import Overlay from "../organisms/Overlay";

const Hero = () => {
  return (
    <div className="hero">
      <Overlay>
        <h1>This is a main Heading</h1>
      </Overlay>
    </div>
  );
};

export default Hero;
